# Instances

    # You’ve already worked with a variety of types in Python, such as str (string). 
    # An individual str value, like "hello", is called an instance or object of that type.

        # str is a type (or class).

        # "hello" is an instance (or object) of the str type.

    # You’ve been creating instances of types like int and str for a while now. 
    # In fact, each value you make in a program is an instance (or object) of its type.

    # The variables hello and a_different_hello are two separate str instances (objects) that happen to hold the same sequence of characters.
#

hello = "hello"
a_different_hello = "hello"

#
    # You’ve seen that the type of an instance determines what kind of data it contains and how you can use it in code.

    # You’ve also performed a few operations on instances: 
        # concatenating strings together using the + operator and doing calculations on numbers. 

    # But instances can be a lot more powerful than that.

    # In this lesson, you’ll learn more about creating and using instances, including accessing their values (attributes) and behaviors (methods).
